import React from "react";

const ContainerBtcGraph = ({ children }) => {
  return (
    <section className="btc-graph">
      <div className="container-btc-graph">{children}</div>
    </section>
  );
};

export { ContainerBtcGraph };
